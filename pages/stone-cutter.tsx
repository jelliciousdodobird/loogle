// styling:
import { css, jsx, Theme, useTheme } from "@emotion/react";
import styled from "@emotion/styled";

const StoneCutter = () => {
  return <div>StoneCutter</div>;
};

export default StoneCutter;
