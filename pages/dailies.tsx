// styling:
import { css, jsx, Theme, useTheme } from "@emotion/react";
import styled from "@emotion/styled";

const DailyTracker = () => {
  return <div>DailyTracker</div>;
};

export default DailyTracker;
